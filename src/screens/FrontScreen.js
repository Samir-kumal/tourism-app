import { View, Text, ImageBackground, SafeAreaView } from "react-native";
import React from "react";

const FrontScreen = () => {
  return (
    <SafeAreaView className="w-full h-20 bg-secondary">
      {/* <ImageBackground>text</ImageBackground> */}
    </SafeAreaView>
  );
};

export default FrontScreen;
